apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: waifu-prod
resources:
  - ../../base
namePrefix: prod-
# Tune replicas/resources after metrics; Postgres/Redis remain external.
