# Profile page assets

Optional background for `profile.html` stage:

- `background.webp` — в этой папке, URL: `/static/game/ui/profile/background.webp`.

If the file is missing, the page falls back to a gradient only.
