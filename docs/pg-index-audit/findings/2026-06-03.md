# Index audit report — 2026-06-03

**Environment:** repository baseline (run on staging/prod after deploy)  
**Migration baseline:** 0096_performance_hot_path_indexes — **yes** (in repo)  
**Auditor:** automated template

## Summary

- [x] Migration 0096 defines partial indexes for hot paths (see below)
- [ ] Run `scripts/pg_index_audit.sql` on live DB and paste EXPLAIN output here
- [ ] Follow-up migration 0097 only if live EXPLAIN shows Seq Scan

## Indexes added by 0096

| Index | Table | Purpose |
|-------|-------|---------|
| `ix_gd_cycles_chat_id_status_active` | `gd_cycles` | `WHERE status = 'active'` per chat |
| `ix_dungeon_progress_player_active` | `dungeon_progress` | active solo `is_active = true` |
| `ix_abyss_progress_player_session_active` | `abyss_progress` | `session_active = true` |
| `ix_gd_cycles_status_created_at` | `gd_cycles` | stuck-cycle ops / registration |

## Expected EXPLAIN (after migrate)

Run on target DB:

```bash
psql "$POSTGRES_URL" -f scripts/pg_index_audit.sql
```

Paste results below when executed:

```
(awaiting live run)
```

## Actions

| Priority | Action | Alembic revision |
|----------|--------|------------------|
| P1 | Apply `0096` on prod: `python -m waifu_bot.cli migrate` | 0096 |
| P2 | Re-run audit after 2 weeks load; add 0097 only from findings | TBD |
